/**
 * Why this approach: a purely presentational hero banner — no Apex, no data.
 * Provides NTO branding and clear CTAs for the portal home page.
 * Navigation uses simple anchor hrefs matching Experience Cloud page names.
 */
import { LightningElement } from "lwc";

export default class NtoHero extends LightningElement {}
